# ==========================================
# AI 游玩日志生成系统 — Python 依赖
# ==========================================

# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0

# 异步文件上传
python-multipart==0.0.19

# 图片处理
Pillow==11.1.0

# HEIC/HEIF 格式支持 (可选，需要先安装 libheif)
# pillow-heif==0.21.0

# HTTP 客户端 (调用 MiMo API)
httpx==0.28.1

# 环境变量
python-dotenv==1.0.1

# CORS 中间件已包含在 fastapi 中

# 数据库 ORM (第一版使用内置 sqlite3)
# 无需额外依赖

# 测试
pytest==8.3.4
pytest-asyncio==0.25.0
httpx  # 已在上方声明，用于 TestClient

# --- 可选: 地点解析 ---
# requests>=2.32.0
